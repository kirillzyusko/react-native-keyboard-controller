// TODO: (x) send message functionality
// TODO: (x) hold position when message added (if list is not inverted done by default - we perform scroll in effect)
// TODO: (x) don't scroll when only few messages visible
// TODO: render first message from bottom from top
// TODO: interactive dismissal
// TODO: customizable behavior when to scroll (i. e. "always", "when bottom reached", "never")
// TODO: inverted support (via menu)
// TODO: FlashList/FlatList integration
// TODO: make sure no re-layout happens on every frame
// TODO: optimized approach for iOS/Android (contentOffset on iOS without changes in every frame)
// TODO: check 120 FPS on all platforms
// TODO: example with sticky footer
